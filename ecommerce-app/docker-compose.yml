version: "3.8"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ecommerce
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD TZ: UTC postgres
      
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data